/* eslint-disable react/prop-types */

export default function Currscore({ Current }) {
  return (
    <section className="current-score">
      <p>Current</p>
      <h2>{Current}</h2>
    </section>
  );
}
